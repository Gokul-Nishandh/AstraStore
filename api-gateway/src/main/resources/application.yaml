spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        # Auth — routed by Docker Compose DNS name inside the astrastore network
        - id: auth-route
          uri: http://auth:8081
          predicates:
            - Path=/api/auth/**

        # Upload Service (PUT object by bucket/key) - must be before metadata to take precedence
        - id: upload-route
          uri: http://upload:8082
          predicates:
            - Path=/api/v1/buckets/{bucketId}/objects/{*key}
            - Method=PUT

        # Download Service (GET/HEAD object by objectId)
        - id: download-by-id-route
          uri: http://download:8083
          predicates:
            - Path=/api/v1/objects/{objectId}
            - Method=GET,HEAD

        # Object DELETE -> metadata service (soft-delete)
        - id: object-delete-route
          uri: http://metadata:8084
          predicates:
            - Path=/api/v1/objects/{objectId}
            - Method=DELETE

        # Download Service (GET/HEAD object by bucket/key - non-greedy match)
        - id: download-by-key-route
          uri: http://download:8083
          predicates:
            - Path=/api/v1/buckets/{bucketId}/objects/{key:.+}
            - Method=GET

        # Download (legacy path)
        - id: download-route
          uri: http://download:8083
          predicates:
            - Path=/api/download/**
          filters:
            - RewritePath=/api/download/(?<segment>.*), /$\{segment}

        # Metadata (Buckets - POST for create, GET for list, DELETE for remove)
        - id: bucket-metadata-write-route
          uri: http://metadata:8084
          predicates:
            - Path=/api/v1/buckets
            - Method=POST
        - id: bucket-metadata-list-route
          uri: http://metadata:8084
          predicates:
            - Path=/api/v1/buckets/**
            - Method=GET,DELETE
        - id: object-metadata-route
          uri: http://metadata:8084
          predicates:
            - Path=/api/v1/objects/**
            - Method=GET

        # Placement
        - id: placement-route
          uri: http://placement:8085
          predicates:
            - Path=/api/placement/**
          filters:
            - RewritePath=/api/placement/(?<segment>.*), /$\{segment}
        # Replication
        - id: replication-route
          uri: http://replication:8086
          predicates:
            - Path=/api/replication/**
          filters:
            - RewritePath=/api/replication/(?<segment>.*), /$\{segment}
        # Monitoring
        - id: monitoring-route
          uri: http://monitoring:8087
          predicates:
            - Path=/api/monitoring/**
          filters:
            - RewritePath=/api/monitoring/(?<segment>.*), /$\{segment}

      # Enable HTTP request metrics for Prometheus
      metrics:
        enabled: true

      # Global CORS — allow Grafana / Jaeger UI to query actuator endpoints
      globalcors:
        corsConfigurations:
          "[/**]":
            allowedOrigins: "*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway,prometheus
  endpoint:
    health:
      show-details: always
  prometheus:
    metrics:
      export:
        enabled: true
  tracing:
    sampling:
      probability: ${MANAGEMENT_TRACING_SAMPLING_PROBABILITY:1.0}

# OTEL / Jaeger
otel:
  exporter:
    otlp:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://jaeger:4317}
  service:
    name: ${OTEL_SERVICE_NAME:api-gateway}

logging:
  level:
    org.springframework.cloud.gateway: DEBUG
    org.springframework.cloud.gateway.route: INFO
